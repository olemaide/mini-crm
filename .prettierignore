.next
node_modules
pnpm-lock.yaml
supabase/.branches
supabase/.temp
# Generated by next-intl's createMessagesDeclaration on every build. The entry
# below used to read `messages.d.ts`, which is not what the plugin emits — so
# `prettier --write` reformatted it, the next build rewrote it, and the diff came
# back. That loop is what the two "en.d.json.ts" commits in the history were.
messages/en.d.json.ts
next-env.d.ts
